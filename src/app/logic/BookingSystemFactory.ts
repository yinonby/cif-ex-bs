

export default class BookingSystemFactory {
  // ********** TASK **********
  /*
    implement a public static method 'buildBookingSystem()' with no arguments
    and a return-type 'BookingSystem'
    - the method calls 'PaymentManagerFactory.buildPaymentManager()' and stores
      the result in a new variable 'paymentOperationsProvider' whose type
       is 'PaymentOperationsProvider'
    - the method returns a new object whose type is BookingSystem, initialized
      using 'paymentOperationsProvider'
  */
}
